# ── Core ──────────────────────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0
timm>=1.0.0
numpy>=1.26.0
Pillow>=10.0.0

# ── Config ────────────────────────────────────────────────────
pyyaml>=6.0

# ── Prisma (HookedViT + pre-trained SAEs) ─────────────────────
# Install from source — pin to a specific commit for reproducibility.
# Replace <commit-hash> with the hash current when you set up the project.
# Find latest commit at: github.com/Prisma-Multimodal/ViT-Prisma/commits/main
# Example: git+https://github.com/Prisma-Multimodal/ViT-Prisma.git@a1b2c3d
git+https://github.com/Prisma-Multimodal/ViT-Prisma.git

# ── CLIP (for automated concept labeling in features.py) ──────
transformers>=4.40.0,<5
open-clip-torch>=2.24.0
wordfreq>=3.1.1

# ── Activation cache ──────────────────────────────────────────
h5py>=3.10.0

# ── Visualisation ─────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
scipy>=1.11.0

# ── Notebooks ─────────────────────────────────────────────────
jupyter>=1.0.0
ipywidgets>=8.0.0

# ── Progress bars ─────────────────────────────────────────────
tqdm>=4.66.0
